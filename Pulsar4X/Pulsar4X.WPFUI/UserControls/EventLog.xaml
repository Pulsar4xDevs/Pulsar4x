<UserControl x:Class="Pulsar4X.WPFUI.EventLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid Name="EventLog_DataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" IsReadOnly="True" Binding="{Binding Time}"/>
                <DataGridTextColumn Header="Faction" IsReadOnly="True" Binding="{Binding Faction}"/>
                <DataGridTextColumn Header="Event Type" IsReadOnly="True" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="System" IsReadOnly="True" Binding="{Binding System}"/>
                <DataGridTextColumn Header="Message" IsReadOnly="True" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
