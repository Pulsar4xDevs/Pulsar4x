<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" x:Class="Pulsar4X.WPFUI.UserControls.ScientistUC" 
             mc:Ignorable="d" d:DesignWidth="300" Height="48">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64*"/>
            <ColumnDefinition Width="69*"/>
            <ColumnDefinition Width="107*"/>
            <ColumnDefinition Width="60*"/>
        </Grid.ColumnDefinitions>
        <Label Content="{Binding ScientistFirstName}" />
        <Label  Grid.Row="0" Grid.Column="1" 
			Content="{Binding ScientistLastName}"/>

        <xctk:ByteUpDown Grid.Column="2" Margin="5,0,0,0"
                            Value="{Binding Path=ScientistAssignedLabs}"/>
        <Label  Grid.Column="3"
                Content="{Binding Path=ScientistMaxLabs}"/>

    </Grid>
</UserControl>
