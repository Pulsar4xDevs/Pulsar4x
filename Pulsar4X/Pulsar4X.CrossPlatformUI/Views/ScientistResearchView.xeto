<?xml version="1.0" encoding="UTF-8"?>
<Panel
  xmlns="http://schema.picoe.ca/eto.forms" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  >
  <TableLayout>
    <TableRow>
      <Label Text="{Binding TechName}" />
      <Label Text="{Binding Level}" />
      <Label Text="{Binding PointsCompleated}" />
      <Label Text="{Binding PointCost}" />
      <!--ProgressBar Value="{Binding PercRemain}" /-->
    </TableRow>    
  </TableLayout>
</Panel>